<?php
// Задание 1: Определение четности чисел. Создайте массив из 5 чисел. Используя цикл foreach и условные операторы ( if-else ), проверьте, является ли каждое число четным или нечетным, и выведите соответствующее сообщение. Четные числа можно найти с помощью операции взятия остатка от деления ( % ). Если число делится на 2 без остатка, оно четное. 

// Создание массива из 5 чисел
$numbers = array(10, 5, 20, 15, 4);
// Цикл foreach для проверки четности
foreach ($numbers as $number) {
    if ($number % 2 == 0) {
        echo "Число $number четное";
    } else {
        echo "Число $number нечетное";
    }
}
// Задание 2: Подсчет положительных и отрицательных чисел. Создайте массив из 7 чисел, содержащий как положительные, так и отрицательные значения. С помощью цикла foreach и ветвления ( if-else ) подсчитайте, сколько в массиве положительных и отрицательных чисел. Используйте условие if ($число > 0) для проверки положительных чисел и else для отрицательных.

$numbers1 = array(10, -5, 20, -15, 4, -2, 0);
// Переменные для подсчета
$positive = 0;
$negative = 0;
// Цикл для подсчета
foreach ($numbers1 as $number) {
 if ($number > 0) {
 $positive++;
 } else {
 $negative++;
 }
}
// Вывод результата
echo "Положительных чисел: " . $positive;
echo "Отрицательных чисел: " . $negative;

// Задание 3: Таблица умножения. Используя цикл for , выведите таблицу умножения для числа 5 от 1 до 10. Каждый результат должен выводиться на новой строке в формате: "5 * 1 = 5" .

// Цикл
for ($i = 1; $i <= 10; $i++) {
  echo "5 * $i = " . ($i * 5);
 }
//  Задание 4: Условное отображение оценок студентов. Создайте массив с именами 3 студентов и их оценками за экзамен (например: ["Иван" => 85, "Мария" => 74, "Алексей" => 90] ). Используя цикл foreach и ветвления ( if-else ), напишите логику, которая определяет, сдал ли каждый студент экзамен:
// Если оценка 50 и выше — вывести: "<Имя студента> сдал экзамен!". Если оценка ниже 50 — вывести: "<Имя студента> не сдал экзамен!".
// Используйте массив вида ["Имя" => оценка] и цикл foreach ($students as $name => $score) , чтобы обрабатывать каждого студента по отдельности.

// Создание массива
$students = array(
  "Иван" => 55,
  "Мария" => 44,
  "Алексей" => 80
 );
  // Цикл
 foreach ($students as $name => $score) {
  if ($score >= 50) {
  echo $name. " сдал(а) экзамен!";
  } else {
  echo $name. " не сдал(а) экзамен!";
  }
 }
// Задание 5: Поиск минимального и максимального значений. Создайте массив из 8 случайных чисел. Используя цикл foreach , найдите и выведите минимальное и максимальное значения в массиве. Создайте две переменные $min и $max , установив их начальные значения на первое число массива. Затем обновляйте эти переменные в зависимости от того, какое число из массива меньше или больше текущих значений. Пример вывода: Минимальное число: 3 Максимальное число: 98
// Создание массива из 8 случайных чисел
$numbers2 = array(15, 4, 35, 58, 84, 16, -5, 0);
// Переменные для значений
$min = $numbers2[0];
$max = $numbers2[0];
// Цикл для поиска значений
foreach ($numbers2 as $number) {
 if ($number < $min) {
 $min = $number;
 }
 if ($number > $max) {
 $max = $number;
 }
}
// Вывод результата
echo "Минимальное число: " . $min;
echo "Максимальное число: " . $max;